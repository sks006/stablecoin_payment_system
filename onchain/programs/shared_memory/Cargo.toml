[package]
name = "shared_memory"
version = "0.1.0"
edition = "2021"

[features]
default = []
anchor-bridge = ["dep:anchor-lang"]
native-bridge = []

[lib]
crate-type = ["rlib"]

[dependencies]
anchor-lang = { version = "0.30.0", optional = true }
