[package]
name = "anchor_stablecoin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[features]
default = []

[dependencies]
anchor-lang = "0.30.0"
shared_memory = { path = "../shared_memory", features = ["anchor-bridge"] }
