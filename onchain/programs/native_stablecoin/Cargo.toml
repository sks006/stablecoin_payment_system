[package]
name = "native-stablecoin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "native_stablecoin"

[dependencies]
solana-program = "1.16"
shared-memory = { path = "../shared_memory", features = ["native-bridge"] }
bytemuck = "1.13"
