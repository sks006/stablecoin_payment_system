apiVersion: v1
kind: Service
metadata:
  name: stablecoin-payment-orchestrator
spec:
  selector:
    app: stablecoin-payment-orchestrator
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
